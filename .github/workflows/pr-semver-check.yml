name: PR SemVer Check

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, labeled, unlabeled, ready_for_review]

permissions:
  contents: read
  pull-requests: read

jobs:
  semver-policy:
    name: SemVer Policy
    uses: JSL-Inc/golden-path-workflows-v2/.github/workflows/pr-semver-check.yml@main
