name: Production Release

on:
  workflow_run:
    workflows: ["Branch Delivery Pipeline"]
    types: [completed]
    branches: [main]

permissions:
  actions: read
  contents: write
  pull-requests: read

jobs:
  production-release:
    name: Production Release
    uses: JSL-Inc/golden-path-workflows-v2/.github/workflows/release.yml@main
