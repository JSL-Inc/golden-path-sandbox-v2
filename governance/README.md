# Governance source

This sandbox does not own the Golden Path rulesets.

The organization ruleset specifications and provisioning workflows live in
`JSL-Inc/golden-path-workflows-v2` on `new-deploy-stuff-template`. Normal
repositories receive them through the `golden_path=enabled` custom property.
No repository ruleset is needed for this sandbox unless it is testing a
documented stricter overlay.
